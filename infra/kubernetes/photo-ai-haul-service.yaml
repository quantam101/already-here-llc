apiVersion: v1
kind: Service
metadata:
  name: photo-ai-haul
  namespace: gmaos
  labels:
    app.kubernetes.io/name: photo-ai-haul
    app.kubernetes.io/part-of: gmaos
    app.kubernetes.io/component: photo-ai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: photo-ai-haul
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
