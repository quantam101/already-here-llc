// app/page.tsx
import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Already Here LLC | Mission-Critical IT Infrastructure & Security',
  // description isn't strictly needed here if layout.tsx has it, 
  // but you can add a custom one for the homepage.
}

export default function Page() {
  return (
    <main>
      {/* Your Hero, Sectors, and Services components go here */}
    </main>
  )
}
