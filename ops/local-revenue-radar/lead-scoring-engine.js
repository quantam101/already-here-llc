export function scoreLead(lead){const weights={revenue:.22,margin:.18,urgency:.15,equipment:.13,skill:.13,distance:.08,repeatability:.07,risk:.04};const score=(lead.revenue*weights.revenue)+(lead.margin*weights.margin)+(lead.urgency*weights.urgency)+(lead.equipment*weights.equipment)+(lead.skill*weights.skill)+(lead.distance*weights.distance)+(lead.repeatability*weights.repeatability)+((100-lead.risk)*weights.risk);return Math.round(score);} export function classify(score){if(score>=85)return 'DO_IT';if(score>=70)return 'REFER_IT';if(score>=55)return 'SELL_IT';return 'IGNORE_IT';}