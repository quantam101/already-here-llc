{
  email {$ACME_EMAIL:dispatch@alreadyherellc.com}
}

alreadyherellc.com, www.alreadyherellc.com {
  encode gzip zstd
  reverse_proxy web:3000

  header {
    Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
    X-Content-Type-Options "nosniff"
    X-Frame-Options "DENY"
    Referrer-Policy "strict-origin-when-cross-origin"
    Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()"
  }
}

# Deferred until DNS records exist for these subdomains.
# app.alreadyherellc.com {
#   encode gzip zstd
#   reverse_proxy web:3000
# }
#
# api.alreadyherellc.com {
#   encode gzip zstd
#   reverse_proxy runtime:8080
# }
#
# status.alreadyherellc.com {
#   encode gzip zstd
#   reverse_proxy uptime-kuma:3001
# }

# Keep n8n private until authentication and access controls are enabled.
# To expose later, uncomment only after setting N8N_BASIC_AUTH_ACTIVE=true or using Cloudflare Access.
# n8n.alreadyherellc.com {
#   encode gzip zstd
#   reverse_proxy n8n:5678
# }
